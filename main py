import telebot
import time
from flask import Flask
from threading import Thread

# ВЕБ-СЕРВЕР БАРОИ RENDER (ки бот хоб наравад)
app = Flask('')

@app.route('/')
def home():
    return "Бот фаъол аст!"

def run():
    app.run(host='0.0.0.0', port=8080)

def keep_alive():
    t = Thread(target=run)
    t.start()

# ТОКЕНИ БОТИ ШУМО
TOKEN = '8664780965:AAFHOqD4daSDAFXF_HAmhtcwsVnS2RYYtCI'
bot = telebot.TeleBot(TOKEN)

# НАМУНАИ ОДДӢ (Шумо метавонед коди фурӯши алмосатонро пурра инҷо гузоред)
@bot.message_handler(commands=['start'])
def start(message):
    bot.reply_to(message, "Салом! Боти шумо дар Render 24/7 кор мекунад!")

# ОҒОЗИ БОТ
if __name__ == "__main__":
    keep_alive()  # Ин веб-серверро дар замина мегиронад
    while True:
        try:
            bot.polling(none_stop=True, interval=0, timeout=20)
        except Exception as e:
            print(f"Error: {e}")
            time.sleep(10)
